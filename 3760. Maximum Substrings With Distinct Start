class Solution {
    public int maxDistinct(String s) {
        HashSet<Character>st=new HashSet<>();
        for(char ch : s.toCharArray()){
            st.add(ch);
        }
        return st.size();
    }
}