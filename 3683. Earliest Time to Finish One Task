class Solution {
    public int earliestTime(int[][] tasks) {
        int min=Integer.MAX_VALUE;
        for(int num []:tasks){
            min=Math.min(min,num[0]+num[1]);
        }
        return min;
    }
}